#include <iostream>

using namespace std;//this is to declare that we are using names from the library files.

int main()
{
    cout << "Hello world!" << endl; /*endl works as \n*/
    cout << "This is a new line use of endl instead escape sequence \\n" <<endl;
    return 0;
}
